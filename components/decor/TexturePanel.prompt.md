Textured section ground — the brand's paper tile repeating at 440×440px. Use it for a full-bleed band that needs warmth without a photograph.

```jsx
<TexturePanel basePath="../../" style={{ padding: "112px 0" }}>…</TexturePanel>
```

Never scale or tint the tile; it repeats at native size only.
