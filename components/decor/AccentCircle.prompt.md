Flat-colour disc. Two jobs: an oversized background shape bleeding off a section edge, and the 56px colour swatch.

```jsx
<AccentCircle size={404.635} fill="sage" />
<AccentCircle size={56} fill="pine" />
```

Discs are always fully opaque flat colour (`clay` is 92% alpha so photography can multiply through it) — never gradients.
