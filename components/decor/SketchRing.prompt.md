Hand-drawn open circle, lifted verbatim from the source file. Loop it around a photo or an accent disc so it breaks the edge — never centre it neatly.

```jsx
<SketchRing variant="a" width={276.639} tone="salmon" basePath="../../" />
```

Tones are pre-tinted SVG files (`salmon`, `slate`, `green`) rather than a CSS-masked shape — ask before adding a new tone, it needs a new file in `assets/marks/`.
